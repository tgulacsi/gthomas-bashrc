#!/bin/sh
arch_user=$(basename $(ls -d /home/{tgulacsi,gthomas} 2>/dev/null))

if which xlock 2>/dev/null; then
  CMD=xlock
else
  CMD='gnome-screensaver-command --lock'
fi

XAUTHORITY=/home/$arch_user/.Xauthority DISPLAY=:0.0 $CMD "$@"
