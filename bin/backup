#!/bin/sh
set -e
exec 2>&1
. "$HOME/.ssh/restic.env"
set -x
restic backup .config .ssh Documents .password-store /etc
find ~/src/ -type f -path '*/.git/config' \
	| restic backup --files-from -
restic forget -H 3 -d 3 -w 3 -m 3
